public struct Memory {
    public base: *void,
    public size: usize
}

public fun newMemory(base: *void, size: usize) -> Memory {
    return struct Memory {
        base: base,
        size: size
    };
}
